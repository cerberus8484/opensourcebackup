# Binaries
/bin/
opensourcebackup-agent-*
*.exe
*.exe~
*.dll
*.so
*.dylib

# Go
*.test
*.out
vendor/

# Secrets — never commit
.env
.env.local
.env.*.local
!.env.example

# Editor
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Build artifacts
dist/
bin/
osb-server-linux
osb-server-*
ChatGPT Image*.png

# Local verification scratch output — reproducible E2E evidence belongs in a dedicated test harness, not here.
docs/agent-runs/

# Interne Sicherheitsdokumentation — nicht öffentlich
docs/security/

# Interne Design-Mockups / Tool-Entwürfe — nicht öffentlich
tooldesign/

# Internes Entwicklungstagbuch — nicht öffentlich
docs/dev_journal.md
docs/dev_journal.html

# Komprimierte Session-/Chat-Zusammenfassungen — nicht öffentlich
docs/session-summaries/

# Agent runtime data (tokens, restore test sandboxes)
data/

# TLS certificates & private keys — never commit (any location)
certs/
*.key
*.pem
*.crt
*.p12
*.pfx
